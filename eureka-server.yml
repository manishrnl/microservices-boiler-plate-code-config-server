server:
  port: 8761
eureka:
  client:
    register-with-eureka: false

    fetch-registry: false

spring:
  application:
    name: eureka-server
  cloud:
    import: optional:configserver:http://config-server:8888
    config:
      enabled: false
      fail-fast: true
